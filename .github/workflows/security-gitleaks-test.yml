name: Security / Gitleaks (Secrets) TEST

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gitleaks:
    name: Case - repository scan succeeds
    uses: ./.github/workflows/security-gitleaks.yml
