name: Security / Trivy FS Runner

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trivy:
    uses: ./.github/workflows/security-trivy.yml
    with:
      RUNNER: ubuntu-latest
      scan_type: image
      image_ref: node:12